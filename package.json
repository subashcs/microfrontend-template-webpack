{
  "name": "module-federation",
  "private": true,
  "workspaces": {
    "packages": [
      "host",
      "app2",
      "app3"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
